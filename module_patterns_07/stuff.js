module.exports.counter = function(arr)
{
    return ('There are ' + arr.length + ' elements in this array.');
};

module.exports.adder = function(a, b)
{
    return 'The sum of of 2 numbers is ${a+b}';
};

modoule.exports.pi = 3.142;
const people = ['Abderazzak', 'Soufiane', 'Oussama', 'Aymane'];
const ages = [22, 22, 21, 22];

console.log(people);

module.exports = {
    people, // or use `classmate: people (so i can refer to people variable as classmate then)
    their_ages: ages
}